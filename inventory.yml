---
# k3s cluster groups
k3s_server:
  hosts:
    kube01.local:

k3s_agent:
  hosts:
    kube02.local:
    kube03.local:
    20-size.local:

k3s_cluster:
  children:
    k3s_server:
    k3s_agent:

# Other systems
cluster:
  hosts:
    20-size.local:

homebridge:
  hosts:
    kube02.local:

toys:
  hosts:
    artpi.local:
    displaypi.local:

cameras:
  hosts:
    backyard:
      ansible_host: 10.0.1.157
    front_porch:
      ansible_host: 10.0.1.163
      amcrest_image_flip: true
      amcrest_image_mirror: true
    driveway:
      ansible_host: 10.0.1.164
  vars:
    ansible_connection: local
