---

all:
    children:

        main:
            hosts:
                cluster0.local:
                    hostname: cluster0
        even:
            hosts:
       
        odd:
            hosts:
                cluster1.local:
                    hostname: cluster1
        ancillary:
            hosts:
                displaypi.local:
                    hostname: displaypi
                    
        cluster_master:
            children:
                    main:

        cluster_workers:
            hosts: cluster1.local
            
        cluster:
            children:
                cluster_master:
                cluster_workers:
 
        workers:
            children:
                even:
                odd:
        
