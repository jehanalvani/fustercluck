---

all:
    hosts: 
        displaypi.local:
#        raspberrypi.local:
    children:
        lab:
            children:
                homebridge:
                    hosts: 
                        cluster0.local:
                monitoring:
                    hosts:
                        cluster1.local:
                database:
                    hosts:
                        cluster2.local:
                grafana:
                    hosts: 
                        cluster1.local:
                statping:
                    hosts:
                        cluster1.local:
                prometheus:
                    hosts:
                        cluster1.local:
                cAdvisor:
                    hosts:
                        cluster0.local:
                        cluster1.local:
                
                        
            
        
